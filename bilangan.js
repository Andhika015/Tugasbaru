let input = prompt("Masukkan Bilangan");
    if ( isNaN(input)){
	 alert(input = "Bukan Bilangan!")
       }else{if(input % 2 === 0){
	      alert(input + "adalah bilangan genap")
       }else{
	      alert(input + "adalah bilangan ganjl")
 }
}